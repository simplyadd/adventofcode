import sys

def main():
    start_pos = 0
    end_pos = 13
    count = 0
    
    with open("input.txt") as fp:
        input = fp.readline()

        for x in input:
            string = ""
            strncop
            myset = set(string)
            
            if (count<len(input)-13) & (count>13):
                if len(myset) == 14:
                    print('first marker after character', d+1)
                    break
                if x in list:
                    a = count + list.index(x)
                    b = a+1
                    c = a+2
                    d = a+3
            count += 1
    fp.close()

if __name__ == "__main__":
    main()